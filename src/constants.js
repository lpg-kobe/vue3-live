/**
* @desc global define during app run
* @author pika
*/

export const { VITE_sdkAppId } = import.meta.env
export const { VITE_apiHost } = import.meta.env
export const { VITE_publicPath } = import.meta.env
export const { MODE } = import.meta.env